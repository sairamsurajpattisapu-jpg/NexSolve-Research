# NexSolve Frontend Environment Configuration
# If unset or empty, Vite proxies /api, /jobs, /health to http://127.0.0.1:8001 in development.
# For production builds, specify the full origin of the deployed backend API:
VITE_API_BASE_URL=http://localhost:8001
